{
  "name": "story-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "root": "apps/story-service",
  "sourceRoot": "apps/story-service/src",
  "tags": ["scope:backend", "type:app"],
  "implicitDependencies": ["common", "contracts", "auth", "database", "redis"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/apps/story-service"],
      "options": {
        "command": "nest build story-service"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest start story-service --watch"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/coverage/apps/story-service"],
      "options": {
        "command": "jest --testPathPattern=apps/story-service --passWithNoTests --coverage=false"
      }
    }
  }
}
